[project]
name = "vm-display"
version = "0.1.0"
description = "Minimal SPICE display client"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
